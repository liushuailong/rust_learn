//! My crate
//!
//! `my crate` is a cellection of utilites to make performing certion calculations more convinent.
//!
/// Add one to a number
///
/// # Example
///
/// ```
/// let x = rl_032_doc::add_one(4);
/// assert_eq!(5, x);
/// ```
pub fn add_one(x: i32) -> i32 {
    x + 1
}
